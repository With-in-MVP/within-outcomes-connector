{
  "name": "within-privacy-bridge",
  "version": "1.1.0",
  "type": "module",
  "description": "Runs in the vendor's environment: pulls CRM/database outcomes, pseudonymizes identifiers locally, pushes only pseudonymous events to Within.",
  "scripts": {
    "start": "node src/bridge.mjs",
    "check": "node --check src/bridge.mjs && node --check src/adapters/salesforce.mjs && node --check src/adapters/postgres.mjs"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
